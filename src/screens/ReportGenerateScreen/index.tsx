import { Button, Layout, Text } from "@ui-kitten/components"

export function ReportGenerate({ navigation }) {

    return (
        <Layout style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text category='h1'>Gerar Relatório</Text>
        </Layout>
    )
}